import React from 'react';
import styles from './footer.module.css';

const Footer = (props) => {
  return (
    <footer className={styles.footer}>
      <p className={styles.title}>design your todo list</p>
    </footer>
  );
};

export default Footer;
